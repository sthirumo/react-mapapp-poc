import React from "react";
import { SafeAreaView } from "react-navigation";
import { Text, StyleSheet } from "react-native";
import Maptrack from "../components/MapTrackView";

const MapScreen = ({ navigation }) => {
  return <Maptrack />;
};

export default MapScreen;
